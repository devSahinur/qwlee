// Re-export the homepage card so /hire-freelancers and the home strip
// stay in lockstep. One file owns the design; this file just keeps
// existing import paths working.

export { default } from "@/components/Home/MostPopularFreelancers/FreelancersCard";
