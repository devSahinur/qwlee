# ---------------------------------------------------------------
# Admin (Vite + React) environment variables. Copy this file to
# `.env` and fill in real values. Vite only exposes vars prefixed
# with VITE_ to the client bundle.
# ---------------------------------------------------------------

# Public URL of the backend API
VITE_API_BASE_URL=http://localhost:7171
